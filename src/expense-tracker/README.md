# expense-tracker

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This project was created using `bun init` in bun v1.3.14. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.

## Build 1 — Expense Rules Engine

- Demonstrates: Strategy Pattern, Open-Closed Principle, rule priority, audit log
- Key design: critical rules separated from priority-sorted rules
- Interview round: Round 4 (Rules Engine Design)" > README.md